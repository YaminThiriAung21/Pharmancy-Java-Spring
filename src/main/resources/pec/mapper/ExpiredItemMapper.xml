<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pec.mapper.ExpiredItemMapper">

	<select resultType="pec.dto.ExpiredItemDto" parameterType="map" id="getExpiredItem">
	     select so.SUPPLIER_DETAIL_ORDER_INFO_ID,
	     pi.product_code, pi.PRODUCT_IMAGE_NAME, pi.product_name, so.expire_date, pi.update_user, pi.update_date from 
	     product_info pi, supplier_detail_order_info so, office_info office where so.product_info_id = pi.product_info_id and
	     pi.office_info_id = office.office_info_id and so.delete_flag = 0 and so.expire_flag = 1 and office.office_info_id =#{office_info_id}
	     and so.expire_date &#60;= #{newDate}
    </select>
	
</mapper>